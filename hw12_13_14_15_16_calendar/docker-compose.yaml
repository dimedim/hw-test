services:
  postgres-db:
    image: postgres:latest
    container_name: postgres_storage
    env_file:
      - .env
    environment:
      POSTGRES_DB: ${DB_NAME}
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data
  rabbitmq:
    image: rabbitmq:management
    container_name: rabbit
    environment:
      RABBITMQ_DEFAULT_USER: guest
      RABBITMQ_DEFAULT_PASS: guest
    ports:
      - "5672:5672"
      - "15672:15672"
    volumes:
      - rabbitmq_data:/var/lib/rabbitmq

  # scheduler:
  #   build: .
  #   command: ["./bin/calendar_scheduler","--config=/etc/calendar/rabbit.yaml"]
  #   depends_on: ["postgres-db","rabbitmq"]
  #   volumes:
  #     - ./configs/rabbit.yaml:/etc/calendar/rabbit.yaml

  # sender:
  #   build: .
  #   command: ["./bin/calendar_sender","--config=/etc/calendar/rabbit.yaml"]
  #   depends_on: ["rabbitmq"]
  #   volumes:
  #     - ./configs/rabbit.yaml:/etc/calendar/rabbit.yaml

volumes:
  db_data:
  rabbitmq_data:
